To do list (*) / ideas (^)
==========================

* renderings of ball with parameters as in [He91] paper
* whitted-style raytracing (simple reflections + direct lighting)
* analyze of SimpleHe model (glossy part and blue hue)
* log rendering parameters along with render filename
* HDR viewer that is using FreeImage
* multi-pass rendering (dumb version)

^ add spectrum definition by color temp and power (for lights)
^ add spectrum definition by RGB #RRGGBB (0->1)
^ add ability to dump all paths to a file and retrieve them for reprocessing
^ decouple serializer from objects